name: test-paths

on: workflow_dispatch

jobs:
    format-checking:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2
            - run: echo "$PATH"
            - run: echo {{ github.workspace }}
